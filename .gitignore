# 进开源仓之前要过一遍：截图里不许出现
#   - 会话列表（里面有作者自己的对话标题）
#   - 本机绝对路径 /Users/...
#   - 未发送的输入框内容、账号、邮箱
# 拍干净截图的流程：新建一个空白会话 → 用 #tmp-shot-mask 把侧栏挡住 → 截图 → 删掉空白会话。
# 旧版带私人信息的截图挪到 ~/.loki-theme-kit/private-previews/，不入仓。

__pycache__/
*.pyc
.DS_Store
.venv/
node_modules/
*.log
